server:
  port: 8000

spring:
  application:
    name: gestao-usuarios
  profiles:
    active: ${AMBIENTE:prod}

  sql:
    init:
      continue-on-error: true

  jpa:
    generate-ddl: false
    show-sql: false

api:
  security:
    token:
      secret: ${JWT_SECRET:silencio}